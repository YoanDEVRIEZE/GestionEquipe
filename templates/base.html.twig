<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Gestion Équipe - {% block title %}{% endblock %}</title>
        <link rel="icon" href="{{ asset('images/favicon.ico') }}">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
        <link href="{{ asset('css/main.css') }}" rel="stylesheet">
        {% block stylesheets %}{% endblock %}
    </head>
    <body class="bg-light">
        <button class="btn btn-dark d-md-none m-2" data-bs-toggle="offcanvas" data-bs-target="#mobileSidebar">
            <i class="bi bi-list"></i>
        </button>
        <nav id="sidebar" class="position-fixed top-0 start-0 vh-100 p-3 d-none d-md-flex flex-column">
            <div class="sidebar-header d-flex flex-column align-items-center mb-4">
                <img src="{{ asset('images/logo.png') }}" alt="Logo" class="mb-2 sidebar-logo">
                <h5 class="text-white m-0 sidebar-title">Gestion Équipe</h5>
            </div>
            <ul class="nav nav-pills flex-column gap-2">
                <li class="nav-item">
                    <a class="nav-link text-white d-flex align-items-center gap-2
                    {{ app.request.attributes.get('_route') == 'gestion_equipe_home' ? 'active' : '' }}"
                    href="{{ path('gestion_equipe_home') }}">
                        <i class="bi bi-speedometer2"></i>
                        <span class="menu-text">Tableau de bord</span>
                    </a>
                </li>
                <li class="nav-item mt-3 mb-1 text-white text-uppercase small sidebar-category">
                    Administrateur
                </li>
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link text-white d-flex align-items-center gap-2
                        {{ app.request.attributes.get('_route') starts with 'gestion_equipe_user_management' ? 'active' : '' }}"
                        href="{{ path('gestion_equipe_user_management_index') }}">
                            <i class="bi bi-people"></i>
                            <span class="menu-text">Utilisateurs</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white d-flex align-items-center gap-2
                        {{ app.request.attributes.get('_route') starts with 'gestion_equipe_position' ? 'active' : '' }}"
                        href="{{ path('gestion_equipe_position_index') }}">
                            <i class="bi bi-briefcase"></i>
                            <span class="menu-text">Postes</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white d-flex align-items-center gap-2
                        {{ app.request.attributes.get('_route') starts with 'gestion_equipe_department' ? 'active' : '' }}"
                        href="{{ path('gestion_equipe_department_index') }}">
                            <i class="bi bi-diagram-3"></i>
                            <span class="menu-text">Services</span>
                        </a>
                    </li>
                {% endif %}
            </ul>
        </nav>
        <div class="offcanvas offcanvas-start bg-dark text-white d-md-none" id="mobileSidebar">
            <div class="offcanvas-header">
                <img src="{{ asset('images/logo.png') }}" alt="Logo">
                <h5 class="offcanvas-title text-white">Gestion Équipe</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas"></button>
            </div>
            <div class="offcanvas-body">
                <ul class="nav nav-pills flex-column gap-2">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('gestion_equipe_home') }}">
                            <i class="bi bi-speedometer2 me-2"></i> Tableau de bord
                        </a>
                    </li>
                    {% if is_granted('ROLE_USER') %}
                        <li class="nav-item mt-2 mb-1 text-white text-uppercase small">Administrateur</li>
                        <li class="nav-item">
                            <a class="nav-link text-white" href="{{ path('gestion_equipe_user_management_index') }}">
                                <i class="bi bi-people me-2"></i> Utilisateurs
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-white" href="{{ path('gestion_equipe_position_index') }}">
                                <i class="bi bi-briefcase me-2"></i> Postes
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-white" href="{{ path('gestion_equipe_department_index') }}">
                                <i class="bi bi-diagram-3 me-2"></i> Services
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
        <main id="main-content" class="p-4 d-flex flex-column min-vh-100">
            {% block main_content %}{% endblock %}
            <div class="mt-auto text-center">
                <small>Version {{ app_version }}</small>
            </div>
        </main>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
