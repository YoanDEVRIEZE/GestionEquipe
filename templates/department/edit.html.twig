{% extends 'base.html.twig' %}

{% block title %}Modifier un service{% endblock %}

{% block main_content %}
    <h1 class="mb-4">
        <i class="bi bi-diagram-3 me-2"></i> Modifier le service {{ department.name }}
    </h1>
    {{ form_start(form) }}
        <div class="d-flex justify-content-end gap-2 mb-3">
            <a href="{{ path('gestion_equipe_department_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
            <button type="submit" class="btn btn-primary">Modifier le service {{ department.name }}</button>
        </div>
        {{ include('department/_form.html.twig') }}
    {{ form_end(form) }}
{% endblock %}
