{% extends 'base.html.twig' %}

{% block title %}Créer un service{% endblock %}

{% block main_content %}
    <h1 class="mb-4">
        <i class="bi bi-diagram-3 me-2"></i> Créer un service
    </h1>
    {{ form_start(form) }}
        <div class="d-flex justify-content-end gap-2 mb-3">
            <a href="{{ path('gestion_equipe_department_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
            <button type="submit" class="btn btn-primary">Créer un service</button>
        </div>
        {{ include('department/_form.html.twig') }}
    {{ form_end(form) }}
{% endblock %}