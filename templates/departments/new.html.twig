{% extends 'base.html.twig' %}

{% block title %}Créer un département ou un service{% endblock %}

{% block main_content %}
    <h1 class="mb-4">Créer un nouveau département ou service</h1>
    {{ form_start(form) }}
        <div class="d-flex justify-content-end gap-2 mb-3">
            <a href="{{ path('gestion_equipe_departments_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
            <button type="submit" class="btn btn-primary">Créer un département ou un service</button>
        </div>
        {{ include('departments/_form.html.twig') }}
    {{ form_end(form) }}
{% endblock %}