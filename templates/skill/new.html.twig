{% extends 'base.html.twig' %}

{% block title %}Créer une compétence{% endblock %}

{% block main_content %}
    <h1 class="mb-4">
        <i class="bi bi-backpack2-fill me-2"></i> Créer une compétence
    </h1>
    {{ form_start(form) }}
        <div class="d-flex justify-content-end gap-2 mb-3">
            <a href="{{ path('gestion_equipe_skill_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
            <button type="submit" class="btn btn-primary">Créer une compétence</button>
        </div>
        {{ include('skill/_form.html.twig') }}
    {{ form_end(form) }}
{% endblock %}